import 'package:chat/api.dart';
import 'package:chat/provider/chat_service.dart';

class Lastmessage {
  final String currentId = Api.useruid;
  final List friend = [];

  Future<void> lastmessage() async {
    for (int i = 0; i <= friend.length; i++) {}
  }
}
